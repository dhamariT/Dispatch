linters-settings:
  errcheck:
    exclude-functions:
      # JSON encoding to an http.ResponseWriter. If this fails the
      # client connection is already broken; there's nothing useful
      # to do with the error.
      - (*encoding/json.Encoder).Encode
      # Only returns on server shutdown; main returns immediately after.
      - net/http.ListenAndServe
